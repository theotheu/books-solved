#Static analyzer for syntax and style check


## Requirements

* [Node.js](http;//nodejs.org)
* npm packages, run `npm install` from this directory.

Run `./run_lint.sh` or `npm test` and check the results in `./error_log.txt`